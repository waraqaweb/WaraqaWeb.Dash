cd C:\waraqa
git status
git add -A
git commit -m "homepage customization 2"
git push origin main

---

cd /opt/waraqa
chmod +x deploy/scripts/deploy.sh
./deploy/scripts/deploy.sh pull
./deploy/scripts/deploy.sh all
./deploy/scripts/deploy.sh auto

cd /opt/waraqa
git fetch origin main
git reset --hard origin/main
docker compose up -d --build frontend
docker compose up -d --build backend

---

cd C:\waraqa
git status
git add -A
git commit -m "plus button and style"
git push origin main
ssh -o BatchMode=yes -o ConnectTimeout=20 -T root@159.89.40.84 "set -e; cd /opt/waraqa; chmod +x deploy/scripts/deploy.sh; ./deploy/scripts/deploy.sh pull; ./deploy/scripts/deploy.sh all; ./deploy/scripts/deploy.sh auto"

ssh -o BatchMode=yes -o ConnectTimeout=20 -T root@159.89.40.84 "set -e; cd /opt/waraqa; for i in \$(seq 1 40); do if ps aux | grep -E 'deploy\.sh|docker compose' | grep -v grep >/dev/null; then echo '[wait] deploy still running, sleeping 15s...'; sleep 15; else break; fi; done; if ps aux | grep -E 'deploy\.sh|docker compose' | grep -v grep >/dev/null; then echo '[abort] deploy still running after wait window'; exit 1; fi; if [ -d /tmp/waraqa-deploy.lock ]; then echo '[info] removing stale deploy lock'; rm -rf /tmp/waraqa-deploy.lock; fi; chmod +x deploy/scripts/deploy.sh; ./deploy/scripts/deploy.sh pull; ./deploy/scripts/deploy.sh all; ./deploy/scripts/deploy.sh auto"

---

## ssl setup on dashboard

and if I click next show all results in the future this is

---

لمعرفة آخر 200 سطر من لوجات الدوكر والـ ssh و الـ dmesg:
journalctl -u docker -n 200 --no-pager
docker compose logs --tail 200
journalctl -u ssh -n 100 --no-pager
dmesg -T | tail -n 100
